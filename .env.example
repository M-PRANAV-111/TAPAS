NEXT_PUBLIC_API_URL=http://localhost:8000
# When the backend is unreachable the client falls back to deterministic demo
# data so the dashboard is still presentable. Set to "false" to fail loudly.
NEXT_PUBLIC_DEMO_FALLBACK=true
